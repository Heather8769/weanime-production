# WeAnime Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=development
PORT=4000

# Supabase Configuration (REQUIRED)
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key-here

# JWT Configuration (REQUIRED)
# Generate a secure random string (32+ characters)
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters-long-please

# Frontend URL (REQUIRED - for CORS and password reset emails)
FRONTEND_URL=http://localhost:3000

# Optional: Direct database connection (not needed for basic setup)
# DATABASE_URL=postgresql://postgres:password@db.your-project-id.supabase.co:5432/postgres

# Optional: Redis for caching (not required for basic setup)
# REDIS_URL=redis://localhost:6379

# Optional: AWS S3 for file uploads (not required for basic setup)
# AWS_S3_BUCKET=your-s3-bucket
# AWS_REGION=us-east-1

# Optional: CDN for video streaming (not required for basic setup)
# CDN_URL=https://your-cdn-domain.com